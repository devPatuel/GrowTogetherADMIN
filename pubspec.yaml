name: growtogetheradmin
description: "Panel de administracion de GrowTogether (web)"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  growtogether_data:
    git:
      url: https://github.com/devPatuel/GrowTogetherDATA.git
      ref: v0.1.0
  dio: ^5.7.0
  provider: ^6.1.2
  intl: ^0.20.2
  fl_chart: ^0.69.0
  data_table_2: ^2.5.16

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.4

flutter:
  uses-material-design: true
